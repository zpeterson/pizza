import { css } from "@microsoft/fast-element";

export const styles = css`
.settings-container {
    display: flex;
    justify-content: flex-end;
    margin: 20px;
    gap: 8px;
}

.select {
    min-width: 120px;
}
`;